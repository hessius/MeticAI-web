# Simple Dockerfile using pre-built dist folder
# Use this if the multi-stage build has issues with npm install
# Build the app first with: npm run build
# Then build the Docker image

FROM nginx:alpine

# Copy pre-built application
COPY dist /usr/share/nginx/html

# Copy example config as a template
COPY config.example.json /usr/share/nginx/html/config.example.json

# Copy nginx configuration
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Expose port 80
EXPOSE 80

# Health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD wget --quiet --tries=1 --spider http://localhost/ || exit 1

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
